<% if @dbname -%>

mysql -h '<%= @host %>' -u'<%= @username %>' -D '<%= @dbname -%>' -p'<%= @password %>' --local-infile=1 -e '<%= @escapedcommand %>'

<% else %>

mysql -h '<%= @host %>' -u'<%= @username %>' -p'<%= @password %>' --local-infile=1 -e '<%= @escapedcommand %>'

<% end -%>
